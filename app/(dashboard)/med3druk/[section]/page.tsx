import { notFound } from "next/navigation";import { SectionPage } from "@/components/section-page";import { medSections } from "@/lib/sections";export default async function Page({params,searchParams}:{params:Promise<{section:string}>;searchParams:Promise<{q?:string;page?:string}>}){const {section}=await params;const config=medSections[section];if(!config)notFound();return <SectionPage project="med3druk" section={config} searchParams={searchParams}/>}
