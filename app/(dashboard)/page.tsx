import { PageHeader } from "@/components/shell"; import { connectionStatus } from "@/lib/config";
export default function Dashboard(){const status=connectionStatus();return <><PageHeader eyebrow="Огляд" title="UKR3DRUK" subtitle="Єдина картина виробництва, клієнтів та цифрових активів."/><div className="card setup"><b>Стан production-підключень</b><p className="muted">Фейкові дані вимкнені. Поки env або міграції не налаштовані, списки порожні, а зміни заблоковані.</p><div className="connections">{Object.entries(status).map(([k,v])=><div className="connection" key={k}><div className="muted">{k.toUpperCase()}</div><p className="status">{v?"Підключено":"Потрібне налаштування"}</p></div>)}</div></div></>}
