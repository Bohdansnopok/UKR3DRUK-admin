import { PageHeader } from "@/components/shell";import { connectionStatus } from "@/lib/config";import { requirePermission } from "@/lib/auth";export default async function Page(){await requirePermission("settings.manage");return <><PageHeader eyebrow="Система" title="Налаштування" subtitle="Підключення та security-параметри без відображення значень секретів."/><div className="grid">{Object.entries(connectionStatus()).map(([k,v])=><div className="card" key={k}><h3>{k}</h3><p className="status">{v?"Готово":"Не налаштовано"}</p><p className="muted">Ключі читаються лише сервером.</p></div>)}</div></>}
