import { PageHeader,Table } from "@/components/shell";import { secureList } from "@/lib/data";import { getSessionProfile } from "@/lib/auth";import { inviteEmployee } from "./actions";export default async function Page(){const rows=await secureList("admin","profiles","team.read");const user=await getSessionProfile();return <><PageHeader eyebrow="Організація" title="Команда" subtitle="Реальні Supabase Auth запрошення та окремі призначення в кожному проєкті."/>{user?.permissions.includes("team.manage")&&<form action={inviteEmployee} className="card invite-form"><label>Ім’я<input className="input" name="name" required/></label><label>Email<input className="input" name="email" type="email" required/></label><label>Проєкт<select className="input" name="project"><option value="med3druk">MED3DRUK</option><option value="calculator">Калькулятор</option></select></label><label>Роль<select className="input" name="role"><option value="viewer">Перегляд</option><option value="production">Виробництво</option><option value="manager">Менеджер</option><option value="co_owner">Співвласник</option></select></label><button className="btn">Надіслати запрошення</button></form>}<div style={{height:16}}/><Table rows={rows}/></>}
