import { NextResponse } from "next/server";import { cookies } from "next/headers";import { createServerClient } from "@supabase/ssr";import { serviceClient } from "@/lib/supabase";import { ownerEmails } from "@/lib/config";
export async function GET(request:Request){const requestUrl=new URL(request.url);const code=requestUrl.searchParams.get("code");const appUrl=process.env.NEXT_PUBLIC_APP_URL??requestUrl.origin;const supabaseUrl=process.env.NEXT_PUBLIC_ADMIN_SUPABASE_URL,anon=process.env.NEXT_PUBLIC_ADMIN_SUPABASE_ANON_KEY;if(!code||!supabaseUrl||!anon)return NextResponse.redirect(`${appUrl}/login?error=callback`);const store=await cookies();const client=createServerClient(supabaseUrl,anon,{cookies:{getAll:()=>store.getAll(),setAll:items=>items.forEach(({name,value,options})=>store.set(name,value,options))}});const {error}=await client.auth.exchangeCodeForSession(code);if(error)return NextResponse.redirect(`${appUrl}/login?error=callback`);const {data:{user}}=await client.auth.getUser();const admin=serviceClient("admin");if(!user?.email||!admin)return NextResponse.redirect(`${appUrl}/login?error=callback`);const configuredOwner=ownerEmails().includes(user.email.toLowerCase());let {data:profile}=await admin.from("profiles").select("id,status,is_owner").eq("id",user.id).maybeSingle();if(configuredOwner){const promoted=await admin.from("profiles").upsert({id:user.id,email:user.email,full_name:user.user_metadata.full_name??user.email,status:"active",is_owner:true}).select("id,status,is_owner").single();profile=promoted.data}const {count}=await admin.from("project_memberships").select("id",{count:"exact",head:true}).eq("user_id",user.id).eq("active",true);if(!profile||profile.status==="inactive"||(!profile.is_owner&&!count)){await client.auth.signOut();return NextResponse.redirect(`${appUrl}/login?error=not-employee`)}const provider=String(user.app_metadata.provider??"");if(provider==="google"){if(profile.status==="invited")await admin.from("profiles").update({status:"active"}).eq("id",user.id);return NextResponse.redirect(appUrl)}return NextResponse.redirect(`${appUrl}/set-password`)}
