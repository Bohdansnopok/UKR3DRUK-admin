import { NotEmployeeModal } from "@/components/not-employee-modal";import { loginWithGoogle } from "./actions";
export default async function Login({searchParams}:{searchParams:Promise<{error?:string}>}){const {error}=await searchParams;const message=error==="not-employee"?"Цей Google-акаунт не має доступу. Увійдіть owner-email або попросіть owner-а додати вас до команди.":null;return <main className="login">{error==="not-employee"&&<NotEmployeeModal/>}<div className="card login-card"><div className="eyebrow">UKR3DRUK</div><h1>Вхід до адмін-панелі</h1><p className="muted">Увійдіть через Google. Після цього доступ отримають лише owner-email та активні запрошені співробітники.</p>{message&&<p className="form-message">{message}</p>}<form action={loginWithGoogle}><button className="btn google-btn" style={{width:"100%"}}>Продовжити з Google</button></form></div></main>}
