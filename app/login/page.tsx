import { NotEmployeeModal } from "@/components/not-employee-modal";import { login,loginWithGoogle } from "./actions";export default async function Login({searchParams}:{searchParams:Promise<{error?:string}>}){const {error}=await searchParams;return <main className="login">{error==="not-employee"&&<NotEmployeeModal/>}<div className="card login-card"><div className="eyebrow">UKR3DRUK</div><h1>Вхід до адмін-панелі</h1><p className="muted">Доступ мають лише співробітники, яких запросив власник.</p><form action={loginWithGoogle}><button className="btn google-btn">G&nbsp;&nbsp; Продовжити з Google</button></form><div className="divider"><span>або через email</span></div><form action={login}><label htmlFor="email">Email</label><input className="input" style={{width:"100%",margin:"8px 0 14px"}} id="email" name="email" type="email" required/><label htmlFor="password">Пароль</label><input className="input" style={{width:"100%",margin:"8px 0 18px"}} id="password" name="password" type="password" required/><button className="btn" style={{width:"100%"}}>Увійти</button></form></div></main>}
