import { activateEmployee } from "./actions";export default function Page(){return <main className="login"><form action={activateEmployee} className="card login-card"><div className="eyebrow">Активація</div><h1>Створіть пароль</h1><p className="muted">Пароль зберігається й обробляється Supabase Auth.</p><label htmlFor="password">Новий пароль</label><input className="input" style={{width:"100%",margin:"8px 0 18px"}} id="password" name="password" type="password" minLength={12} required/><button className="btn" style={{width:"100%"}}>Активувати акаунт</button></form></main>}
