import { PageHeader,Table } from "./shell";import type { Section } from "@/lib/sections";import { secureList } from "@/lib/data";import { CalculatorOrderTable } from "./order-table";import { getSessionProfile } from "@/lib/auth";import { MedCrudTable,medCrudFields } from "./med-crud-table";import { WorkspaceSettingsTable } from "./workspace-settings-table";import type { Permission } from "@/lib/rbac";
const managePermissions:Record<string,Permission>={orders:"med3druk.orders.manage",catalog_products:"med3druk.products.manage",crm_clients:"med3druk.clients.manage",crm_centers:"med3druk.centers.manage",crm_partners:"med3druk.partners.manage",model_files:"med3druk.models.manage",deliveries:"med3druk.deliveries.manage",documents:"med3druk.documents.manage",workspace_settings:"med3druk.settings.manage"};
export async function SectionPage({project,section,searchParams}:{project:"med3druk"|"calculator";section:Section;searchParams:Promise<{q?:string;page?:string}>}){const sp=await searchParams,rows=await secureList(project,section.table,section.permission,Number(sp.page)||1,sp.q??""),user=await getSessionProfile(),managePermission=managePermissions[section.table],canManage=Boolean(managePermission&&user?.permissions.includes(managePermission));let content=<Table rows={rows}/>;if(section.table==="print_orders")content=<CalculatorOrderTable rows={rows} canManage={Boolean(user?.permissions.includes("calculator.orders.manage"))}/>;if(project==="med3druk"&&section.table==="workspace_settings")content=<WorkspaceSettingsTable rows={rows} canManage={canManage}/>;else if(project==="med3druk"&&section.table in medCrudFields&&canManage)content=<MedCrudTable table={section.table} rows={rows}/>;return <><PageHeader eyebrow={project} title={section.title} subtitle={section.subtitle}/><form className="toolbar"><label className="sr-only" htmlFor="search">Пошук</label><input id="search" name="q" defaultValue={sp.q} className="input" placeholder="Пошук…"/><button className="btn">Знайти</button></form>{content}</>}
